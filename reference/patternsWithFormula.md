# Patterns valid to compute daily dose with the associated formula.

Patterns valid to compute daily dose with the associated formula.

## Usage

``` r
patternsWithFormula
```

## Format

A data frame with eight variables: `pattern_id`, `amount`,
`amount_unit`, `numerator`, `numerator_unit`, `denominator`,
`denominator_unit`, `formula_name` and `formula`.
